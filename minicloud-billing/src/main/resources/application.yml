server:
  port: 8085

spring:
  application:
    name: minicloud-billing
  datasource:
    url: jdbc:mysql://localhost:3306/minicloud_billing?createDatabaseIfNotExist=true
    username: minicloud_admin
    password: minicloud_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
